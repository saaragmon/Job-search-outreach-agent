[project]
name = "job-search-outreach-agent"
version = "0.1.0"
description = "Multi-agent job outreach: research, CV match, reviewer, send, track"
requires-python = ">=3.12"
dependencies = [
    "gradio>=5.0.0",
    "openai-agents>=0.2.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "pypdf>=5.0.0",
]
